# Bounded ranges; requirements.lock.txt holds the resolved set builds use.
#   uv pip compile requirements.txt -o requirements.lock.txt
# Keep in sync with [project.dependencies] in pyproject.toml.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
python-dotenv>=1.0.1,<2.0
httpx>=0.27,<1.0
PyGithub>=2.4,<3.0

langgraph>=1.0,<2.0
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-openai>=1.0,<2.0
langchain-anthropic>=1.0,<2.0

# Each major changed the callback API; tracing.py targets v3.
langfuse>=3.0,<4.0

structlog>=24.4,<26.0
